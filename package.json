{
  "name": "kurlydeer-workspace",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:clave": "npm run dev --workspace=apps/clave",
    "dev": "npm run dev --workspaces --if-present"
  }
}
